### Fishing mall end to end tests
 
This framework is built with cucumber and serenity.

Install gherkin and cucumber plugins.

In order to run all the tests execute the following command:
`mvn clean verify -Dit.test=runner`


The test results can be found under /target/site/serenity/index.html.